# Admin UI rules

- Admin API base: /admin/api/*
- При создании/апдейте items строго следовать канону assets:
  svg/<id>.svg, nc/<id>.nc, preview/<id>.<ext>  (без ведущего "/")
- UI может быть простым: приоритет — поток наполнения каталога.

